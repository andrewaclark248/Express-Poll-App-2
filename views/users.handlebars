<div class="row">
   <div class="col-lg-3 offset-lg-3">
       <center><button type="button" class="btn btn-primary">Add Admin User</button></center>
    </div>
    <div class="col-lg-3">
       <center><button type="button" class="btn btn-primary">Add General User</button></center>
    </div>

</div>
<br>
<div class="row">
    <div class="col-lg-8 offset-lg-2">
        <div class="card">
            <div class="card-header">Header</div>
            <div class="card-body">
                <table class="table">
                    <thead>
                        <tr>
                        <th scope="col">#</th>
                        <th scope="col">First</th>
                        <th scope="col">Last</th>
                        <th scope="col">Handle</th>
                        </tr>
                    </thead>
                    <p>this value = {{users.count}}</p>
                    <tbody>
                        {{#each users as |user| }}
                            <tr>
                                <th scope="row">1</th>
                                <td> {{ this.user_name }}</td>
                                <td>Otto</td>
                                <td>@mdo</td>
                            </tr>
                        {{/each}} 
                    </tbody>
                    </table>
            </div>
        </div>
    </div>


</div>

