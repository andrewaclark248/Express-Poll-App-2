/**var cookieJwtAuthGeneralUser = require('../services/cookieJwtAuthGeneralUser')

const express = require('express');
const router = express.Router();

router.get('/', cookieJwtAuthGeneralUser, index)

module.exports = router;


function index (req, resp) {
    resp.redirect("general_user_polls");
  };
  ***/